alias gti='git'
#alias tmux='tmux -2'
alias less='less -R'
alias diff='colordiff'
alias dc='cd'
alias dd='dd status=progress'

# ssh quick connects
alias sulu='ssh henning@sulu.node.unbk.me'
alias beta='ssh henning@beta.node.unbk.me'
alias urogo='ssh henning@beta.node.unbk.me'

alias glog='git log --oneline --graph --color --all --decorate'
# squash commits to merge base "develop" or parameter
function gitsc() {
        if [ -n "$1" ]
        then
                git reset $(git merge-base "$1" $(git branch --show-current))
        else
                git reset $(git merge-base develop $(git branch --show-current))
        fi
}

alias goldwarden='flatpak run --command=goldwarden com.quexten.Goldwarden'

function bw-add-key() {
  flatpak run --command=goldwarden com.quexten.Goldwarden ssh add --name "$1"
}
